### 18.4Sum
* Given an array *nums* of n integers and an integer *target*, 
  are there elements a, b, c, and d in *nums* such that 
  a + b + c + d = *target*? Find all unique quadruplets in the array
  which gives the sum of *target*.
* **Note**

  The solution set must not contain duplicate quadruplets.
  
  (返回的集合中不能出现相同的四元组)
* **Example**:
  >Given array nums = [1, 0, -1, 0, -2, 2], and target = 0.<br>
  A solution set is:<br>
  [<br>
  &nbsp;&nbsp;[-1,  0, 0, 1],<br>
  &nbsp;&nbsp;[-2, -1, 1, 2],<br>
  &nbsp;&nbsp;[2,  0, 0, 2]<br>
  ]
  
* **解题思路**
  
  4Sum是在3Sum的基础上完成的，只需在外层再套一层循环即可。

  解题过程中注意除去重复的解。

  在两层for循环中加入每次检查数列的**前四个数之和**以及
  **第一个数和后三个数之和**的判断。这样可以快速的跳出
  不符合条件的循环，加快运行速度，实际测试中，运行速度由
  40ms变为12ms，效果明显。

  ![运行效果比较](/18-4Sum_resouce/18-1.png)<br>
  <img src="18-4Sum_resouce/18-1.png/" alt="运行效果比较">

